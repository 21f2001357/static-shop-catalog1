name: IITM Assignment Action

# This triggers the action every time you push to the main branch
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Checks out your repository under $GITHUB_WORKSPACE
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Your specific required step
      - name: 21f2001357@ds.study.iitm.ac.in
        run: echo "GitHub Action is running successfully for SKPByahut"

      # Step 3: Optional - useful for your ML projects
      - name: Python Version Check
        run: python --version
